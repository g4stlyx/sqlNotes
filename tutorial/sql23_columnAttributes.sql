USE sql_store;

/*

you can reach them from schemas => sql_store(db) => Tables
then choose a table and click to the second button, whose icon is kind of a tool(wrench)
you will see those respectively:

column names, datatypes, primary key, 
nn(not null): can the field be null?
uq(unique): unique index
ai(auto increment): used with id's generally, increases the id when a user added  
default/expression: default value of the field (if exist)


datatypes: 
	int, date,
    varchar(50) : string with max length of 50 characters -short for variable character-




*/